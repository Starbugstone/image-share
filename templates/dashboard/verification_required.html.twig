{% extends 'base.html.twig' %}

{% block title %}Email Verification Required - ImageShare{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card">
            <div class="card-body p-5">
                <div class="text-center mb-4">
                    <i class="fas fa-envelope-open-text fa-4x text-warning mb-3"></i>
                    <h1 class="card-title">Email Verification Required</h1>
                    <p class="text-muted">You need to verify your email address before you can access your dashboard.</p>
                </div>

                <div class="alert alert-warning">
                    <h5 class="alert-heading">
                        <i class="fas fa-exclamation-triangle me-2"></i>Action Required
                    </h5>
                    <p class="mb-2">
                        Hello <strong>{{ user.username }}</strong>, your account is not yet verified.
                        Please check your email (<strong>{{ user.email }}</strong>) for the verification link.
                    </p>
                    <p class="mb-0">
                        If you haven't received the email, you can request a new verification link below.
                    </p>
                </div>

                <div class="text-center mb-4">
                    <a href="{{ resend_url }}" class="btn btn-primary btn-lg">
                        <i class="fas fa-redo me-2"></i>Resend Verification Email
                    </a>
                </div>

                <div class="alert alert-info">
                    <h6 class="alert-heading">
                        <i class="fas fa-info-circle me-2"></i>What happens after verification?
                    </h6>
                    <ul class="mb-0">
                        <li>You'll gain full access to your dashboard</li>
                        <li>You can upload and manage images</li>
                        <li>You can create and share albums</li>
                        <li>You can participate in the community</li>
                    </ul>
                </div>

                <div class="text-center">
                    <p class="text-muted mb-2">Already verified your email?</p>
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-secondary">
                        <i class="fas fa-sign-out-alt me-2"></i>Logout and Login Again
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
